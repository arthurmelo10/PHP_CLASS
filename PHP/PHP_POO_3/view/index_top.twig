// bootstrap está sendo carregado via CDN
// fonte está sendo carregado via CDN

// arquivo myStyle está passando de forma direta

// Necessidade de passar uma nnURL válida -> setar de forma dinâmica

//atualizar o caminho da chamada do arquivo mystyle => <link rel="stylesheet" href="{{THEME}}/css/mystyle.css">

//atualizar o caminho da chamada do imagemdo topo => <img src="{{THEME}}/img/logo·png" ...>

// imagens da listagem de notícias precisam ser alteradas, entretanto serão atu8alizadas via conexão com banco de dados